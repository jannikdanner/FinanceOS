{
  "name": "finance-os",
  "private": true,
  "version": "1.0.0",
  "description": "FinanceOS — Bloomberg Terminal Clone",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n SERVER,VITE -c blue,green \"node server.js\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "concurrently": "^9.0.0"
  }
}
